#include <iostream>

using namespace std;

int main()
{
    int number = 6;
    cout << "This is untidy code. I'm surprised it works." << endl;
    cout << "It has " << number << " lines of code - each more hideous than the last." << endl;
    cout << "You should probably add some line breaks in the text too." << endl;
    cin.get();
    return 0;
}
